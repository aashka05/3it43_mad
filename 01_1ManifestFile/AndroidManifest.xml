<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.a01_1manifestfile">

    <!--<uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="33" /> -->

    <uses-permission
        android:name="android.permission.CAMERA"
        android:maxSdkVersion="18" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._01_1ManifestFile"
        tools:targetApi="31" >
        <!--android:debuggable="false"-->

        <uses-library android:name="android.test.runner" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-configuration
        android:reqTouchScreen="finger"
        android:reqNavigation="trackball"
        android:reqHardKeyboard="true"
        android:reqKeyboardType="qwerty"/>
        <!-- OR android:reqKeyboardType="twelvekey" -->
    <permission
        android:name="com.pad.DETONATE_DEVICE"
        android:protectionLevel="dangerous"
        android:label="Self Destruct"
        android:description="@string/detonate_description">
        <!--add in res/values/strings.xml:
        <string name="detonate_description">Detonate button</string> -->
    </permission>
</manifest>